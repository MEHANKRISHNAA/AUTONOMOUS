[pytest]
# `pytest -q` (unlike `python -m pytest -q`) does not add the current working
# directory to sys.path, so `from app.main import app` in tests/test_api.py
# fails with ModuleNotFoundError outside an editable install. This is the
# minimal fix - no package install, no CI command change.
pythonpath = .
